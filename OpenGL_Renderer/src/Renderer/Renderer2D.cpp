#include "oglrpch.h"
#include "Renderer2D.h"

void Renderer2D::InitRenderer2D()
{
	
}

void Renderer2D::PrepareScene()
{
	
}

void Renderer2D::Draw()
{
	DrawQuad();
}

void Renderer2D::DrawQuad()
{
	/*quadData->va->Bind();
	quadData->texture->Bind();
	quadData->shader->Bind();
	Call(glDrawElements(GL_TRIANGLES, quadData->ib->GetCount(), GL_UNSIGNED_INT, nullptr));*/
}
